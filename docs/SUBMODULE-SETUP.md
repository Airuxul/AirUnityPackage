# Submodule 说明

本地路径带 `com.air.` 前缀（如 `com.air.unity-ui`）；GitHub 仓库名通常不带此前缀（如 `unity-ui`）。

`com.air.unity-connector` 位于 [unity-cli](https://github.com/Airuxul/unity-cli) 的 `com.air.unity-connector/` 目录。

初始化：`init-submodules.bat`

列表见 [PACKAGES.md](PACKAGES.md)。
